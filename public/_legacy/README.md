# Legacy static site (archived)

These files were the original static HTML/CSS/JS portfolio. The site is
now a Next.js App Router project under `src/app/`. This directory is
preserved only for reference; nothing here is routed.

To remove permanently, delete this directory. Git history retains it.

The ontology data (`public/_legacy/js/ontology-data.js`) was also copied
into `src/content/projects/theseus/ontology-data.raw.ts` for porting to
the Theseus principle schema.
